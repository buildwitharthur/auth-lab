{
    "name": "authlab",
    "private": true,
    "scripts": {
        "dev": "pnpm -r --parallel --if-present run dev",
        "build": "pnpm -r --if-present run build",
        "dev:web": "pnpm --filter @authlab/web run dev",
        "dev:api": "pnpm --filter @authlab/api run dev"
    }
}
